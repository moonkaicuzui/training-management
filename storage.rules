rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Quality blog images: authenticated users can upload, anyone can read
    match /quality-blog/images/{imageId} {
      allow read: if true;
      allow write: if request.auth != null
                   && request.resource.size < 5 * 1024 * 1024
                   && request.resource.contentType.matches('image/.*');
    }

    // System feedback screenshots: authenticated users can upload, anyone can read
    match /system-feedback/screenshots/{imageId} {
      allow read: if true;
      allow write: if request.auth != null
                   && request.resource.size < 5 * 1024 * 1024
                   && request.resource.contentType.matches('image/.*');
    }

    // Metal shoe defect photos: authenticated users can upload, anyone can read
    match /metal-shoe-defects/{imageId} {
      allow read: if true;
      allow write: if request.auth != null
                   && request.resource.size < 10 * 1024 * 1024
                   && request.resource.contentType.matches('image/.*');
    }

    // Metal shoe action evidence photos: authenticated users can upload, anyone can read
    match /metal-shoe-actions/{caseId}/{imageId} {
      allow read: if true;
      allow write: if request.auth != null
                   && request.resource.size < 10 * 1024 * 1024
                   && request.resource.contentType.matches('image/.*');
    }

    // Default: deny all
    match /{allPaths=**} {
      allow read, write: if false;
    }
  }
}
